describe("Calculs", function() {
    it("additionne correctement", function() {
        expect(1 + 1).toBe(2);
    });

    it("convertit bien en nombre", function() {
        expect(Number("5")).toBe(5);
    });
});